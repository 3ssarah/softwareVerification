//java 플러그인을 추가한다.
 apply plugin: 'java'
 apply plugin: 'idea'

sourceSets{
    main{
    output.classesDir "$buildDir/clasees"
    }
}
task(run, dependsOn: 'classes', type: JavaExec){
    main="HelloWorld"
    classpath = sourceSets.main.runtimeClasspath
}
